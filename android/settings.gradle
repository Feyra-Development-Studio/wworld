// Проект приложения под Android. Библиотека берётся из подмодуля, а не
// готовым файлом: собирать из исходников важно затем, чтобы правки в форке
// доезжали до приложения сразу, без ручного переноса.
pluginManagement {
    repositories { google(); mavenCentral(); gradlePluginPortal() }
}
dependencyResolutionManagement {
    repositories { google(); mavenCentral() }
}
rootProject.name = "wworld"
include ':app'
