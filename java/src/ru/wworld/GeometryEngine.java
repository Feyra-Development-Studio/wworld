package ru.wworld;

/** Движок геометрии: строка запроса — строка ответа.
 *
 *  Тот же протокол, что у процесса Rscript, но здесь движок живёт внутри
 *  приложения. Это не сервер: ничего не слушает и никуда не подключается,
 *  просто вызов метода. */
public interface GeometryEngine {
    String ask(String command);
}
